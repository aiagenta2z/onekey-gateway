# References

This folder is generated by `agtm skills build`.

- `api_list.json`: Raw `api_list` fetched from the marketplace (or loaded from `agent.yaml` / `agent.json`).
- `api_meta.json`: Normalized API metadata used by the scripts in `scripts/`.

